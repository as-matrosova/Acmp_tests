В Python не принято писать переменные с большой буквы.
Оператор range не включает последнее значение, поэтому в диапазоне прописываем (1, n)
PyCharm, IntelliJ IDEA
VS code
Jupiter Notebook		- можно взять облачное IDE

n = 100
m = n*(n-1)/2
1) Нужно получить m строчек со всеми комбинациями i от 1 до n-1, j от i+1 до n

2) Нужно сделать цикл по i от 1 до n-1
	и для каждого i сделать цикл по j от i+1 до n
		для каждого j вывести пару i, j

n = 100
m = n*(n-1)/2
for i in range(1, n):
	for j in range(i+1, n+1):
		print(i, j)

3) Нужно получить одну строчку из n чисел n-1, разделенных пробелами

n = 100
z = n-1
with open("output.txt", "w") as file_2
	for k in range(n)
		file_2.write(str(z) + " ")


открыть файл

прочитать первую строчку и распаковать в n, m 

сделать массив из n элементов и заполнить его нулями

в цикле m раз:
	читать строчку и распаковать в i, j
	в массиве в ячейке с номером i-1 увеличить значение на 1
	в массиве в ячейке с номером j-1 увеличить значение на 1

создать новый файл и записать туда в строчку полученный массив